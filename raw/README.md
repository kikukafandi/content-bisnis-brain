# Raw Sources

Immutable source-of-truth documents. The LLM reads from here and never edits here.

- Drop articles (e.g. via Obsidian Web Clipper), papers, data, or notes here.
- Put images in `raw/assets/`, downloaded locally so the LLM can view them.
- Then ask the agent to **ingest** the source into the wiki.